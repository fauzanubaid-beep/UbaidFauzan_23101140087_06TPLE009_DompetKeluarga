<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/md_theme_light_background">

    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="64dp"/>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_tambah"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_anchor="@id/bottom_nav"
        app:layout_anchorGravity="top|end"
        android:layout_marginEnd="20dp"
        android:translationY="-32dp"
        android:src="@drawable/ic_action_add"
        app:backgroundTint="@color/color_yellow"
        app:tint="@color/color_heading"
        app:elevation="0dp"
        app:fabCustomSize="52dp"
        app:shapeAppearanceOverlay="@style/ShapeAppearanceOverlay.App.CornerSize50Percent"/>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/color_card_border"
        app:layout_anchor="@id/bottom_nav"
        app:layout_anchorGravity="top"/>

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_nav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="72dp"
        android:layout_gravity="bottom"
        android:background="@color/nav_bg"
        app:menu="@menu/bottom_nav_menu"
        app:itemIconTint="@drawable/nav_color_selector"
        app:itemTextColor="@drawable/nav_color_selector"
        app:itemRippleColor="@null"
        app:itemActiveIndicatorStyle="@style/App.Custom.Indicator"
        app:labelVisibilityMode="labeled"
        app:itemTextAppearanceActive="@style/App.Custom.BottomNav.Active"
        app:itemTextAppearanceInactive="@style/App.Custom.BottomNav.Inactive"
        app:elevation="0dp"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
